provider google {
  # version = "~> 2.11"
  project = var.project
  region  = "asia-northeast1"
}

provider google-beta {
  # version = "~> 2.11"
  project = var.project
  region  = "asia-northeast1"
}